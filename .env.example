# ============================================================
# 应用基础配置
# ============================================================
APP_NAME=NodeLaravel
APP_ENV=development
APP_DEBUG=true
APP_URL=http://localhost

# ============================================================
# 加密密钥 [必须] 生产环境必须通过环境变量配置
# 生成方式（Linux/macOS）:
#   APP_KEY=$(openssl rand -hex 32)
#   APP_IV=$(openssl rand -hex 16)
# ============================================================
APP_KEY=
APP_IV=

# ============================================================
# HTTP/HTTPS 配置
# ============================================================
HTTP_PORT=80
HTTPS_PORT=443
SSL_ENABLED=false
SSL_KEY_PATH=../certs/server.key
SSL_CERT_PATH=../certs/server.crt

# ============================================================
# 时区
# ============================================================
TIME_ZONE=Asia/Shanghai

# ============================================================
# 上传文件存储路径
# ============================================================
UPLOAD_PATH=storage/uploads

# ============================================================
# 数据库配置
# ============================================================
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=node_laravel
DB_USER=root
DB_PASSWORD=
DB_PREFIX=

# ============================================================
# 缓存配置
# 推荐生产环境使用 Redis，避免 Cluster 多进程文件冲突
# ============================================================
CACHE_DRIVER=file   # file | redis | sqlite
CACHE_HOST=127.0.0.1
CACHE_PORT=6379
CACHE_PASSWORD=
CACHE_PREFIX=nl:
CACHE_PATH=storage/cache

# ============================================================
# Cluster 集群配置
# ============================================================
CLUSTER_ENABLED=false
# auto = 自动使用所有 CPU 核心；也可以指定具体数字如 4
CLUSTER_WORKERS=auto

# ============================================================
# WebSocket 配置
#   standalone: 独立进程模式（生产环境推荐，配合 Cluster）
#   shared:     与 HTTP 共享进程（简单场景）
# ============================================================
WS_ENABLED=false
WS_PORT=8080
WS_PATH=/socket.io
WS_MODE=shared

# ============================================================
# 前端服务（静态资源代理）
# ============================================================
FRONT_PORT=3000

# ============================================================
# 安全配置
# ============================================================
VERIFY_SIGNATURE=false
REQUEST_ENCRYPT=false
RETURN_ENCRYPT=false
# Token 有效期（秒），默认 30 分钟
TOKEN_TIME=1800

# ============================================================
# 日志配置
#   debug:  开发环境，输出详细日志
#   info:   生产环境，推荐
#   warn:   仅警告和错误
#   error:  仅错误
# ============================================================
LOG_LEVEL=debug

# ============================================================
# CORS 白名单配置
#   多个域名用逗号分隔，* 表示允许所有（仅开发环境）
# ============================================================
CORS_ORIGINS=*
